export interface ExchangeInfo {
  spentAmount: number;
  spentToken: string;
  receiveToken: string;
  expectedAmountAsNumber: number;
  expectedAmountAsString: string;
  expectedFeeAsNumber: number;
  expectedFeeAsString: string;
}
